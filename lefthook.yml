pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: bunx oxlint .
    - name: typecheck
      run: bunx tsc --noEmit

commit-msg:
  jobs:
    - name: commitlint
      run: bunx commitlint --edit {1}
